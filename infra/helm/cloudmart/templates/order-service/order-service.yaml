apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 5003
      targetPort: 5003
